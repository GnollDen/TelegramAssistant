# mcp/AGENTS.md — Legacy MCP notes

## This directory

This directory is kept for legacy/auxiliary notes only.
Active MCP implementation lives in `src/TgAssistant.Mcp/`.

## Build & verify (active MCP project)

```bash
cd src/TgAssistant.Mcp
npm install
npm run build
npm run dev
```

For active conventions, use `src/TgAssistant.Mcp/index.ts` and repository-level `AGENTS.md`.
