# TgAssistant.Web

This folder is retained only as a legacy diagnostic web shell during cleanup.

It is not the active operator workflow and it is not the target implementation for PRD Stage 6, Stage 7, or Stage 8.

The clean-slate interface track will rebuild operator-facing surfaces on top of the new person-first knowledge model.
