# Orchestration Master Plan (Phase-B Completion Run)

Date: `2026-04-06`  
Mode: `single-active-agent sequential orchestration`  
Scope: `authority-driven planning and task-pack regeneration for missing AI-centric implementation`

## 1. Current-State Synthesis

- Active planning authority root is `docs/planning/README.md`.
- Active product authority chain is:
  - `PERSON_INTELLIGENCE_SYSTEM_PRD_2026-04-02.md`
  - `LLM_PROVIDER_GATEWAY_PRD_2026-04-03.md`
  - `OPERATOR_INTERACTION_LAYER_PRD_2026-04-03.md`
  - `AI_CENTRIC_CONTEXT_LOOP_ADDENDUM_2026-04-05.md`
- Planning-input source for Phase-B task generation:
  - `AI_CENTRIC_REQUIREMENTS_SUPPLEMENT_2026-04-06.md` (not direct execution authority by itself)
- `AI_CONFLICT_RESOLUTION_SESSION_DESIGN_2026-04-06.md` is currently `proposed bounded design`, not auto-promoted execution authority.
- `tasks.json` and `task_slices.json` are structurally complete and fully `done`.
- Existing `docs/planning/DETAILED_IMPLEMENTATION_TASK_PACK_2026-04-06.md` (DTP-001..015) is bounded hardening/surface closure oriented.
- Required file `docs/planning/PROJECT_AGENT_RULES_2026-04-06.md` is restored in Git (`e068bcb`, `2026-04-06`) and included in authority checks.

## 2. Gap Mapping Against AI-Centric Target

The current completed task pack is insufficient against the supplement-required Phase-B outcomes:

1. Temporal Person-State Modeling (`B1`) is not execution-decomposed to implementation-ready bounded tasks.
2. Iterative Pass Reintegration (`B2`) is not execution-decomposed with carry-forward identity/status contracts.
3. AI Conflict Resolution Session Execution (`B3`) is present as proposed design only, not promoted implementation task authority.
4. Current World Approximation (`B4`) is not decomposed into bounded read model + update/recompute tasks.
5. Conditional Preference and Behavior Modeling (`B5`) lacks bounded model and verification slices.
6. Stage 6/7/8 Semantic Contracts (`B6`) are not decomposed into enforceable boundaries and acceptance checks.

## 3. DTP Coverage Map (What Current Pack Covers / Does Not Cover)

### Covered by existing DTP-001..015 (Phase A)

- LoopV1 bounded runtime settings, usage capture, and budget gating.
- Guardrails/rollback proofing and failure taxonomy hardening.
- Offline-event bounded evidence admission for operator flows.
- Label/trust parity in operator surfaces.
- Web home/dashboard API and smoke closure.

### Not covered by existing DTP-001..015 (must be Phase B)

- Temporal validity and supersession state model for person knowledge.
- Multi-pass unresolved-case carry-forward and reintegration loop.
- Executable AI conflict-resolution session lifecycle with deterministic apply boundary.
- Current world-state approximation as first-class modeled output.
- Conditional preference/behavior representation with exceptions and phase shifts.
- Explicit enforceable Stage6/Stage7/Stage8 semantic ownership contracts.

## 4. Phase Split

- `Phase A (already covered)`: bounded AI-loop hardening + operator-surface closure.
- `Phase B (this run output target)`: bounded AI-centric person/world modeling and case-resolution execution to close supplement requirement areas `B1..B6`.

## 5. Workstream Decomposition For Missing AI-Centric Parts

1. `WS-B1 Temporal Person-State Model`
   - temporal fields, validity windows, supersession chain, event-conditioned updates.
2. `WS-B2 Iterative Pass Reintegration`
   - case identity, carry-forward statuses, reintegration triggers, targeted recompute hooks.
3. `WS-B3 AI Conflict Session Execution`
   - start/respond/finalize session lifecycle, bounded tooling, deterministic normalization handoff.
4. `WS-B4 Current World Approximation`
   - active/inactive actor state around subject, current relationship state surface, temporal recency policy.
5. `WS-B5 Conditional Preferences & Behavior`
   - condition clauses, exception representation, style/phase drift markers, bounded evidence linkage.
6. `WS-B6 Stage 6/7/8 Semantic Contract Clarification`
   - enforceable boundary contracts, no stage blending, acceptance tests for ownership separation.

## 6. Documentation Update Plan

Planned deliverable updates in this run:

1. `docs/planning/MASTER_PROJECT_STATUS_2026-04-06.md`
   - rewrite to include explicit Phase-A-done/Phase-B-open framing.
2. `docs/planning/DETAILED_IMPLEMENTATION_TASK_PACK_2026-04-06.md`
   - keep as Phase-A historical baseline (no destructive rewrite).
3. `docs/planning/DETAILED_IMPLEMENTATION_TASK_PACK_PHASE_B_2026-04-06.md`
   - create new execution-ready bounded task pack for `B1..B6`.
4. `docs/planning/COMPACT_EXECUTION_CONTEXT_2026-04-06.md`
   - refresh with latest truths, decisions, closed/open set, next order.
5. Orchestration evidence docs (new in this run):
   - `docs/planning/ORCH_INTEGRATION_LOG_2026-04-06_PHASE_B.md`
   - `docs/planning/ORCH_CRITICAL_POINT_LOG_2026-04-06_PHASE_B.md`

## 7. Sequential Agent Execution Plan (One Agent At A Time)

1. `business-analyst`
   - objective: authoritative gap matrix and Phase-A coverage map versus `B1..B6`.
2. `architect-reviewer`
   - objective: target architecture boundaries + dependency graph across `WS-B1..WS-B6`.
3. `llm-architect`
   - objective: bounded AI-loop/session/retrieval contracts for B2/B3/B6 with deterministic boundaries.
4. `database-administrator`
   - objective: bounded data/read-model ownership for temporal/conditional/world-state requirements.
5. `backend-developer`
   - objective: code-area scoping and implementation-slice mapping per workstream.
6. `ux-researcher`
   - objective: operator-facing flow constraints for escalation/resolution outputs.
7. `project-manager`
   - objective: intern-safe detailed task pack generation with strict dependency order.
8. `reviewer`
   - objective: strict contradiction/risk/ambiguity gate before final packaging.
9. `technical-writer`
   - objective: finalize compact docs package wording and handoff consistency.

Rule: after each agent result, update orchestration log + critical-point log + compact context before next spawn.

## 8. Final Consistency / Audit Pass

Completion gate for this run:

1. Authority consistency check against:
   - planning README
   - active PRDs/addendum/supplement
   - proposed-doc status boundaries
   - backlog files
2. Task-pack quality check:
   - bounded scope
   - explicit dependencies
   - step-by-step instructions
   - verification and acceptance
   - risks / do-not-do / artifacts
   - no vague tasks
3. AI-centric target check:
   - explicit coverage of `B1..B6`
4. Weak-executor readiness check:
   - no ambiguous ownership
   - no hidden assumptions
   - no broad rewrite tasks
